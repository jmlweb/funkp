/**
 * Return the first item in an array
 *
 * @func
 * @param {Array} - Array to get the first item of
 * @return {*}
 * 
 * @example
 * const array = ['a', 'b', 'c'];
 * head(array) // 'a'
 */
const head = ([x]) => x;

export default head;